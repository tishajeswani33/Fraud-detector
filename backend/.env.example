# ML Model directory (relative to where you run the server)
MODEL_DIR=./models

# Comma-separated list of allowed CORS origins
ALLOW_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Uvicorn settings
HOST=0.0.0.0
PORT=8000
